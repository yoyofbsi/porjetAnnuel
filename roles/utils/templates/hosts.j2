127.0.0.1   localhost
127.0.1.1   {{ ansible_hostname }}
{% for _host in groups["all"] %}
#{{ hostvars[_host]["ansible_default_ipv4"]["address"] }}    {{ hostvars[_host]["inventory_hostname"] }}
{% endfor %}
{% for _host in groups["all"] %}
{{ hostvars[_host].ansible_host }}    {{ hostvars[_host]["ansible_hostname"] }}
{% endfor %}

# The following lines are desirable for IPv6 capable hosts
::1     localhost ip6-localhost ip6-loopback
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters